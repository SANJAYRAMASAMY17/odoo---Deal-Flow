        {/* =========================================================================
            MODULE 3: APPROVALS (LIST & DETAIL) — Wireframes #5 and #6
           ========================================================================= */}
        {activeModule === 'Approvals' && (
          activeApprovalDetail ? (
            /* WIREFRAME #6 APPROVAL DETAIL VIEW */
            <div className="space-y-6">
              {/* Back navigation & Title Bar */}
              <div className="flex flex-col md:flex-row md:items-center justify-between gap-4 border-b border-slate-800 pb-5">
                <div>
                  <button
                    onClick={() => setActiveApprovalDetail(null)}
                    className="text-xs text-amber-400 hover:text-amber-300 font-semibold flex items-center gap-1.5 mb-2 transition-colors cursor-pointer"
                  >
                    <span>←</span>
                    <span>Back to Approvals List</span>
                  </button>
                  <h1 className="text-3xl font-extrabold tracking-tight text-white font-display">
                    Approval Detail: {activeApprovalDetail.quotationId} ({activeApprovalDetail.customer})
                  </h1>
                  <p className="mt-1 text-sm text-slate-400">
                    Opened by clicking a row on the Approvals list
                  </p>
                </div>

                {/* Badges from Wireframe #6 */}
                <div className="flex items-center gap-3">
                  {/* Blended Risk Pill */}
                  <span
                    className={`px-4 py-1.5 rounded-full text-xs font-extrabold tracking-wider border shadow-md ${
                      activeApprovalDetail.blendedRisk === 'HIGH'
                        ? 'bg-rose-500/20 text-rose-300 border-rose-500/50 shadow-rose-500/10'
                        : activeApprovalDetail.blendedRisk === 'MEDIUM'
                        ? 'bg-amber-500/20 text-amber-300 border-amber-500/50 shadow-amber-500/10'
                        : 'bg-emerald-500/20 text-emerald-300 border-emerald-500/50 shadow-emerald-500/10'
                    }`}
                  >
                    Blended Risk: {activeApprovalDetail.blendedRisk}
                  </span>

                  {/* Customer Tier Pill */}
                  <span className="px-4 py-1.5 rounded-full text-xs font-extrabold tracking-wider bg-sky-500/20 text-sky-300 border border-sky-500/50 shadow-md shadow-sky-500/10">
                    Customer Tier: {activeApprovalDetail.customerTier || 'Gold'}
                  </span>
                </div>
              </div>

              {/* Section: Why This Quote Was Flagged */}
              <div className="space-y-3">
                <h3 className="text-lg font-bold font-display text-white">
                  Why This Quote Was Flagged
                </h3>

                {/* Flagged Line Items Table matching Wireframe #6 */}
                <div className="glass-card rounded-2xl border border-slate-700/80 overflow-hidden shadow-xl">
                  <table className="w-full text-left text-sm text-slate-300">
                    <thead className="bg-slate-900/90 text-xs font-semibold text-slate-400 border-b border-slate-800">
                      <tr>
                        <th className="py-3.5 px-6">Line</th>
                        <th className="py-3.5 px-6">Discount Given</th>
                        <th className="py-3.5 px-6">Limit Allowed</th>
                        <th className="py-3.5 px-6">Over By</th>
                      </tr>
                    </thead>
                    <tbody className="divide-y divide-slate-800/70">
                      {activeApprovalDetail.flaggedLines && activeApprovalDetail.flaggedLines.map((item, idx) => (
                        <tr key={idx} className="hover:bg-slate-800/30 transition-colors">
                          <td className="py-3.5 px-6 font-semibold text-white">
                            {item.line}
                          </td>
                          <td className="py-3.5 px-6 font-mono font-bold text-slate-200">
                            {item.discount}
                          </td>
                          <td className="py-3.5 px-6 font-mono text-slate-400">
                            {item.limit}
                          </td>
                          <td className="py-3.5 px-6">
                            <span
                              className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-extrabold ${
                                item.isOver
                                  ? 'bg-rose-500/20 text-rose-300 border border-rose-500/40'
                                  : 'bg-emerald-500/20 text-emerald-300 border border-emerald-500/40'
                              }`}
                            >
                              {item.overBy}
                            </span>
                          </td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>

                {/* Explanatory Rule Banner matching Wireframe #6 */}
                <div className="p-4 rounded-2xl bg-amber-500/10 border border-amber-500/40 text-amber-200 text-xs sm:text-sm font-medium flex items-center gap-3 shadow-lg">
                  <div className="w-7 h-7 rounded-lg bg-amber-500/20 text-amber-400 flex items-center justify-center shrink-0">
                    <svg className="w-4 h-4" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
                    </svg>
                  </div>
                  <span>
                    {activeApprovalDetail.flaggedNote || 'Worst single line (8pt over) plus overall pattern across the order sets the blended score. One bad line is enough to require approval.'}
                  </span>
                </div>
              </div>

              {/* Multi-Stage Workflow Stepper (Process Flow matching Wireframe #6) */}
              <div className="glass-card rounded-2xl p-6 sm:p-8 border border-slate-700/80 shadow-xl">
                <div className="text-xs font-bold uppercase tracking-wider text-slate-400 mb-6 flex items-center justify-between">
                  <span>Approval Lifecycle Pipeline</span>
                  <span className="text-blue-400 font-mono text-[11px]">
                    Current: Stage {((activeApprovalDetail.currentStageIndex ?? 1) + 1)} of {(activeApprovalDetail.workflowStages?.length || 4)}
                  </span>
                </div>

                <div className="flex items-center justify-between max-w-2xl mx-auto relative px-4">
                  {(activeApprovalDetail.workflowStages || ['Submitted', 'Sales Manager', 'Finance', 'Confirmed']).map((stageName, idx) => {
                    const currentIdx = activeApprovalDetail.currentStageIndex ?? 1;
                    const isCompleted = idx < currentIdx;
                    const isCurrent = idx === currentIdx;

                    return (
                      <div key={stageName} className="flex items-center flex-1 last:flex-none">
                        {/* Circle Node */}
                        <div className="flex flex-col items-center relative z-10">
                          <div
                            className={`w-11 h-11 rounded-full flex items-center justify-center font-bold text-xs transition-all ${
                              isCompleted
                                ? 'bg-emerald-500 text-slate-950 ring-4 ring-emerald-500/20 shadow-lg shadow-emerald-500/20'
                                : isCurrent
                                ? 'bg-blue-500 text-white shadow-xl shadow-blue-500/40 ring-4 ring-blue-500/30 animate-pulse'
                                : 'bg-slate-800 border-2 border-slate-700 text-slate-500'
                            }`}
                          >
                            {isCompleted ? (
                              <svg className="w-5 h-5 text-slate-950 stroke-[3]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path strokeLinecap="round" strokeLinejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                              </svg>
                            ) : (
                              <span>{idx + 1}</span>
                            )}
                          </div>
                          <span
                            className={`mt-2.5 text-xs font-bold text-center whitespace-nowrap ${
                              isCurrent
                                ? 'text-blue-400 font-extrabold'
                                : isCompleted
                                ? 'text-emerald-400'
                                : 'text-slate-500'
                            }`}
                          >
                            {stageName}
                          </span>
                          {isCurrent && (
                            <span className="text-[10px] text-blue-300 bg-blue-500/20 px-2 py-0.5 rounded-full border border-blue-500/30 mt-1 font-semibold">
                              Active Stage
                            </span>
                          )}
                        </div>

                        {/* Connector Line & Arrow */}
                        {idx < (activeApprovalDetail.workflowStages?.length || 4) - 1 && (
                          <div className="flex-1 flex items-center mx-2 -mt-6">
                            <div
                              className={`h-0.5 flex-1 transition-all ${
                                isCompleted ? 'bg-emerald-500' : 'bg-slate-700'
                              }`}
                            />
                            <svg
                              className={`w-3.5 h-3.5 -ml-1 ${
                                isCompleted ? 'text-emerald-500' : 'text-slate-700'
                              }`}
                              fill="currentColor"
                              viewBox="0 0 20 20"
                            >
                              <path
                                fillRule="evenodd"
                                d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z"
                                clipRule="evenodd"
                              />
                            </svg>
                          </div>
                        )}
                      </div>
                    );
                  })}
                </div>
              </div>

              {/* Audit History Table matching Wireframe #6 */}
              <div className="space-y-3">
                <div className="flex items-center justify-between">
                  <h3 className="text-base font-bold font-display text-white">Audit History</h3>
                  <span className="text-xs text-slate-400 font-mono">Chronological Compliance Log</span>
                </div>

                <div className="glass-card rounded-2xl border border-slate-700/80 overflow-hidden shadow-xl">
                  <table className="w-full text-left text-sm text-slate-300">
                    <thead className="bg-slate-900/90 text-xs font-semibold text-slate-400 border-b border-slate-800">
                      <tr>
                        <th className="py-3 px-6">User</th>
                        <th className="py-3 px-6">Action</th>
                        <th className="py-3 px-6">Date</th>
                        <th className="py-3 px-6">Note</th>
                      </tr>
                    </thead>
                    <tbody className="divide-y divide-slate-800/70 font-sans">
                      {activeApprovalDetail.auditHistory && activeApprovalDetail.auditHistory.map((item, idx) => (
                        <tr key={idx} className="hover:bg-slate-800/30 transition-colors">
                          <td className="py-3.5 px-6 font-semibold text-white">
                            {item.user}
                          </td>
                          <td className="py-3.5 px-6">
                            <span
                              className={`inline-flex items-center px-2.5 py-0.5 rounded-md text-xs font-bold ${
                                item.action === 'Submitted' || item.action === 'Resubmitted'
                                  ? 'bg-sky-500/15 text-sky-300 border border-sky-500/30'
                                  : item.action === 'Returned' || item.action === 'Returned for Revision'
                                  ? 'bg-amber-500/15 text-amber-300 border border-amber-500/30'
                                  : item.action === 'Rejected'
                                  ? 'bg-rose-500/15 text-rose-300 border border-rose-500/30'
                                  : 'bg-emerald-500/15 text-emerald-300 border border-emerald-500/30'
                              }`}
                            >
                              {item.action}
                            </span>
                          </td>
                          <td className="py-3.5 px-6 font-mono text-xs text-slate-400">
                            {item.date}
                          </td>
                          <td className="py-3.5 px-6 text-xs text-slate-300">
                            {item.note}
                          </td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              </div>

              {/* Bottom Action Controls matching Wireframe #6 */}
              <div className="glass-card rounded-2xl p-6 border border-slate-700 flex flex-col md:flex-row items-center justify-between gap-4">
                <button
                  onClick={() => {
                    const targetQuote = quotations.find((q) => q.id === activeApprovalDetail.quotationId) || quotations[0];
                    openQuotationDetail(targetQuote);
                  }}
                  className="text-xs text-amber-400 hover:underline font-semibold flex items-center gap-1 cursor-pointer"
                >
                  <span>Inspect Line Items in Quotation Detail ({activeApprovalDetail.quotationId})</span>
                  <span>→</span>
                </button>

                <div className="flex items-center gap-3 w-full md:w-auto justify-end">
                  {/* Approve (Green) */}
                  <button
                    onClick={handleDetailApprove}
                    className="py-3 px-8 rounded-xl font-bold text-sm text-white bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-500 hover:to-teal-500 shadow-xl shadow-emerald-600/25 transition-all transform active:scale-95 cursor-pointer"
                  >
                    Approve
                  </button>

                  {/* Return for Revision (Amber) */}
                  <button
                    onClick={handleDetailReturnForRevision}
                    className="py-3 px-7 rounded-xl font-bold text-sm text-slate-950 bg-amber-500 hover:bg-amber-400 shadow-xl shadow-amber-500/25 transition-all transform active:scale-95 cursor-pointer"
                  >
                    Return for Revision
                  </button>

                  {/* Reject (Red) */}
                  <button
                    onClick={handleDetailReject}
                    className="py-3 px-7 rounded-xl font-bold text-sm text-white bg-rose-600 hover:bg-rose-500 shadow-xl shadow-rose-600/25 transition-all transform active:scale-95 cursor-pointer"
                  >
                    Reject
                  </button>
                </div>
              </div>
            </div>
          ) : (
            /* WIREFRAME #5 APPROVALS (LIST) VIEW */
            <div className="space-y-6">
              {/* Header matching Wireframe #5 */}
              <div className="flex flex-col md:flex-row md:items-center justify-between gap-4">
                <div>
                  <h1 className="text-3xl font-extrabold tracking-tight text-white font-display">
                    Approvals (List)
                  </h1>
                  <p className="mt-1 text-sm text-slate-400">
                    Every quotation that needed, needs, or is going through discount approval
                  </p>
                </div>

                <div className="flex flex-wrap items-center gap-3">
                  {/* Quick Action to open Q-1042 Approval Detail directly */}
                  <button
                    onClick={() => openApprovalDetailView(approvals[0])}
                    className="py-2 px-3.5 rounded-xl font-semibold text-xs text-white bg-gradient-to-r from-amber-500 to-rose-600 hover:from-amber-400 hover:to-rose-500 shadow-md shadow-amber-500/20 flex items-center gap-1.5 transition-all cursor-pointer"
                  >
                    <span>Open Q-1042 Approval Detail</span>
                    <span>→</span>
                  </button>

                  {/* Status Metric Filter Badges from Wireframe #5 */}
                  <div className="flex items-center gap-2">
                    {/* 3 Pending Badge */}
                    <button
                      onClick={() => setApprovalFilter(approvalFilter === 'Pending' ? 'ALL' : 'Pending')}
                      className={`py-2 px-3.5 rounded-xl text-xs font-bold transition-all flex items-center gap-2 cursor-pointer ${
                        approvalFilter === 'Pending'
                          ? 'bg-amber-500 text-slate-950 shadow-lg shadow-amber-500/25 ring-2 ring-white/30'
                          : 'bg-amber-500/20 hover:bg-amber-500/30 text-amber-300 border border-amber-500/40'
                      }`}
                    >
                      <span className="w-2 h-2 rounded-full bg-amber-400" />
                      <span>{pendingCount} Pending</span>
                    </button>

                    {/* 1 Returned Badge */}
                    <button
                      onClick={() => setApprovalFilter(approvalFilter === 'Returned' ? 'ALL' : 'Returned')}
                      className={`py-2 px-3.5 rounded-xl text-xs font-bold transition-all flex items-center gap-2 cursor-pointer ${
                        approvalFilter === 'Returned'
                          ? 'bg-rose-500 text-white shadow-lg shadow-rose-500/25 ring-2 ring-white/30'
                          : 'bg-rose-500/20 hover:bg-rose-500/30 text-rose-300 border border-rose-500/40'
                      }`}
                    >
                      <span className="w-2 h-2 rounded-full bg-rose-400" />
                      <span>{returnedCount} Returned</span>
                    </button>

                    {/* 12 Approved Badge */}
                    <button
                      onClick={() => setApprovalFilter(approvalFilter === 'Approved' ? 'ALL' : 'Approved')}
                      className={`py-2 px-3.5 rounded-xl text-xs font-bold transition-all flex items-center gap-2 cursor-pointer ${
                        approvalFilter === 'Approved'
                          ? 'bg-emerald-500 text-slate-950 shadow-lg shadow-emerald-500/25 ring-2 ring-white/30'
                          : 'bg-emerald-500/20 hover:bg-emerald-500/30 text-emerald-300 border border-emerald-500/40'
                      }`}
                    >
                      <span className="w-2 h-2 rounded-full bg-emerald-400" />
                      <span>{approvedCount} Approved</span>
                    </button>
                  </div>
                </div>
              </div>

              {/* Approvals Table (5 Columns matching Wireframe #5) */}
              <div className="glass-card rounded-2xl border border-slate-700/80 overflow-hidden shadow-2xl">
                <div className="overflow-x-auto">
                  <table className="w-full text-left text-sm text-slate-300">
                    <thead className="bg-slate-900/90 text-xs font-semibold text-slate-400 border-b border-slate-800">
                      <tr>
                        <th className="py-4 px-6">Quotation</th>
                        <th className="py-4 px-6">Customer</th>
                        <th className="py-4 px-6">Blended Risk</th>
                        <th className="py-4 px-6">Stage</th>
                        <th className="py-4 px-6">Assigned To</th>
                      </tr>
                    </thead>
                    <tbody className="divide-y divide-slate-800/70 font-sans">
                      {filteredApprovals.map((item) => (
                        <tr
                          key={item.id}
                          onClick={() => openApprovalDetailView(item)}
                          className="hover:bg-slate-800/40 transition-colors cursor-pointer group"
                        >
                          {/* Quotation ID */}
                          <td className="py-4 px-6 font-mono text-sm font-bold text-white group-hover:text-amber-400 transition-colors">
                            {item.quotationId}
                          </td>

                          {/* Customer */}
                          <td className="py-4 px-6 font-semibold text-white">
                            {item.customer}
                          </td>

                          {/* Blended Risk (HIGH / MEDIUM / LOW) */}
                          <td className="py-4 px-6">
                            <span
                              className={`inline-flex items-center px-3 py-1 rounded-full text-xs font-extrabold tracking-wider border ${
                                item.blendedRisk === 'HIGH'
                                  ? 'bg-rose-500/20 text-rose-300 border-rose-500/40'
                                  : item.blendedRisk === 'MEDIUM'
                                  ? 'bg-amber-500/20 text-amber-300 border-amber-500/40'
                                  : 'bg-emerald-500/20 text-emerald-300 border-emerald-500/40'
                              }`}
                            >
                              {item.blendedRisk}
                            </span>
                          </td>

                          {/* Stage */}
                          <td className="py-4 px-6 text-xs font-medium text-slate-300">
                            {item.stage}
                          </td>

                          {/* Assigned To */}
                          <td className="py-4 px-6 font-medium text-slate-200">
                            {item.assignedTo}
                          </td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              </div>

              {/* Callout Banner matching Wireframe #5 */}
              <div className="p-4 rounded-2xl bg-amber-500/10 border border-amber-500/30 text-amber-200 text-xs sm:text-sm font-medium flex items-center justify-between gap-3">
                <div className="flex items-center gap-2.5">
                  <span className="text-base">💡</span>
                  <span>Click any row to open its full approval detail, risk breakdown, and audit trail.</span>
                </div>
                <span className="text-xs text-amber-400/80 font-mono hidden sm:inline">Row Selection Active</span>
              </div>

              {/* Filter: Pending Only Button matching Wireframe #5 */}
              <div className="flex items-center gap-3">
                <button
                  onClick={() => setApprovalFilter(approvalFilter === 'Pending' ? 'ALL' : 'Pending')}
                  className={`py-3 px-6 rounded-xl text-xs sm:text-sm font-semibold border transition-all cursor-pointer ${
                    approvalFilter === 'Pending'
                      ? 'bg-amber-500 text-slate-950 border-amber-400 shadow-lg shadow-amber-500/20'
                      : 'bg-slate-800/80 hover:bg-slate-700 text-slate-300 border-slate-700'
                  }`}
                >
                  {approvalFilter === 'Pending' ? '✓ Showing: Pending Only' : 'Filter: Pending Only'}
                </button>

                {approvalFilter !== 'ALL' && (
                  <button
                    onClick={() => setApprovalFilter('ALL')}
                    className="text-xs text-slate-400 hover:text-white transition-colors"
                  >
                    Clear Filter (Show All)
                  </button>
                )}
              </div>
            </div>
          )
        )}